#include <stdio.h>
#include <stdlib.h>

struct coordenada{
  int x;
  int y;}

int main(){
struct coordenada c;
  scanf("%d", &c.x);
  scanf("%d", &c.y);
  
printf("Distancia da coordenada inserida ate a origem eh %d,%d",c.x,c.y);

return 0;}
